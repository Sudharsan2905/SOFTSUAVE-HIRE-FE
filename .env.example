# Application name
VITE_APP_NAME=Talentia

# Vite dev server proxy target — Node-side only, not sent to browser
VITE_PROXY_TARGET=http://localhost:8000

# LiveKit server address — used server-side by the Vite proxy only (not sent to browser)
VITE_LIVEKIT_SERVER=ws://localhost:7880

# Google OAuth Client ID (from Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
